query economicResource($id: ID!) {
  economicResource(id: $id) {
    id
    name
    note
    image
    currentLocation {
          id
          name
          lat
          long
        }
    onhandQuantity {
      id,
      hasNumericalValue
      hasUnit {
        id
        label
      }
    }
  }
}