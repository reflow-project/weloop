#mutation anonResetPassword ($email: String!){
#    requestResetPassword(password: $email){
#    ...AuthData
#  }
#}
#

# request an email to be sent to reset my password if I forgot it
mutation anonResetPasswordRequest($email: String!){
    requestResetPassword(email: $email)
}

mutation anonLogin ($email: String!, $password: String!){
    login(
        emailOrUsername: $email
        password: $password
    ) {
        token
        currentUsername
        currentAccountId
        currentUser {
            id
            profile {
                name
                summary
            }
        }
    }
}

# request another email confirmation to be sent
mutation requestConfirmEmail($email: String!) {
    requestConfirmEmail(email: $email)
}

# confirm my email address with the token that was emailed
mutation confirmEmail($token: String!) {
    confirmEmail(token: $token) {
        accountId
    }
}

mutation anonSignUp($email: String!, $password: String!) {
    signup(email: $email, password: $password)
}
